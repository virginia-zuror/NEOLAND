const mixedElements = [6, 1, 'Rayo', 1, 'vallecano', '10', 'upgrade', 8, 'hub'];
//function averageWord(param) {
  // insert code
//}
let sum;

for (const mixedElement of mixedElements) {
    if(isNaN(mixedElements.mixedElement) == false){
        sum += mixedElements.mixedElement;
        mixedElements.mixedElement ++;
        console.log(sum);
    }else if (isNaN(mixedElement)) {
        parseInt(mixedElement);
        sum += mixedElements.mixedElement;
    };
}